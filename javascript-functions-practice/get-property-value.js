/* exported getPropertyValue */
/**
 *
 * @param {object} object a Javascript object
 * @param {string} key a string
 * @returns The value stored in the object at the specified property key.
 */
function getPropertyValue(object, key) {
  return object[key];
}
